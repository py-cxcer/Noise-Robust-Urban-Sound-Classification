# Verified on Windows with Python 3.13 and an NVIDIA GTX 1660 Ti.
# The NVIDIA driver may advertise a newer CUDA version because PyTorch wheels
# bundle their own runtime. These three versions must remain matched.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.11.0+cu126
torchvision==0.26.0+cu126
torchaudio==2.11.0+cu126

-r requirements.txt

